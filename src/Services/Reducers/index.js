import {combineReducers} from 'redux'
import cardItems from './reducer'

export default combineReducers({
    cardItems
    // if we had more reducers we would have just given a comma and combined the reducers.
})